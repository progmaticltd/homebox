# Generated from HomeBox preseed
LANGUAGE={{ locale.id }}.{{ locale.charset }}
LC_ALL={{ locale.id }}.{{ locale.charset }}
LANG={{ locale.id }}.{{ locale.charset }}
LC_CTYPE={{ locale.id }}.{{ locale.charset }}