# RoundCube milters configuration
[main]
debug={{ system.debug or webmail.milters.debug }}

[postgres]
user=roundcube_ro
password={{ roundcube_db_ro_password }}
dbName=roundcube

# For testing pursposes
# [sqlite]
# path=/var/lib/milter-abook/addresses.db