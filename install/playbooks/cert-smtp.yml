---

# Install and configure certificate for postfix
- hosts: homebox
  vars_files:
    - '{{ playbook_dir }}/../../config/system.yml'
  vars:
    certificate:
      type: smtp
  roles:
    - certificates
