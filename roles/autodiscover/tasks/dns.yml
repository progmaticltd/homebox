---

- name: Create the main DNS record
  vars:
    dns_records:
      - name: autodiscover
        type: CNAME
        value: '{{ network.hostname }}.{{ network.domain }}'
  include_role:
    name: dns-record
  tags: dns
