---

- name: Create the certificate
  vars:
    certificates:
      - name: autodiscover
  include_role:
    name: certificates

- name: Copy the autodiscover certificates in the debian standard directory
  file:
    src: '{{ file.src }}'
    dest: '{{ file.dest }}'
    state: link
  loop:
    - src: /var/lib/lego/certificates/autodiscover.{{ network.domain }}.issuer.crt
      dest: /etc/ssl/certs/autodiscover.{{ network.domain }}.issuer.crt
    - src: /var/lib/lego/certificates/autodiscover.{{ network.domain }}.crt
      dest: /etc/ssl/certs/autodiscover.{{ network.domain }}.crt
    - src: /var/lib/lego/certificates/autodiscover.{{ network.domain }}.key
      dest: /etc/ssl/private/autodiscover.{{ network.domain }}.key
  loop_control:
    loop_var: file
