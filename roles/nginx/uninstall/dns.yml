---

- name: Remove DNS entry for www
  vars:
    dns_records:
      - name: www
        value: main.{{ network.domain }}
        type: cname
  include_role:
    name: dns-record
    tasks_from: uninstall/main.yml
