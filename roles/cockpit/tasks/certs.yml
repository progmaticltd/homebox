---

- name: Copy the certificates in the debian standard directory
  copy:
    src: '{{ file.src }}'
    dest: '{{ file.dest }}'
    owner: root
    group: root
    mode: 0644
    remote_src: yes
  loop:
    - src: /var/lib/lego/certificates/cockpit.{{ network.domain }}.issuer.crt
      dest: /etc/ssl/certs/cockpit.{{ network.domain }}.issuer.crt
    - src: /var/lib/lego/certificates/cockpit.{{ network.domain }}.crt
      dest: /etc/ssl/certs/cockpit.{{ network.domain }}.crt
  loop_control:
    loop_var: file

- name: Copy the private key in the debian standard directory
  copy:
    src: /var/lib/lego/certificates/cockpit.{{ network.domain }}.key
    dest: /etc/ssl/private/cockpit.{{ network.domain }}.key
    remote_src: yes
    owner: root
    group: ssl-cert
    mode: 0640
