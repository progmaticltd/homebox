---

- name: Create the main DNS record
  vars:
    dns_records:
      - name: autoconfig
        type: CNAME
        value: '{{ network.hostname }}.{{ network.domain }}'
  include_role:
    name: dns-record
  tags: dns
