---

- name: Install the required packages
  apt:
    name: [ swaks, libnet-ssleay-perl ]
    state: present

- name: Check each postfix package
  shell: >-
    dpkg -s {{ pkg }}
  loop: '{{ postfix_pkgs }}'
  loop_control:
    loop_var: pkg
