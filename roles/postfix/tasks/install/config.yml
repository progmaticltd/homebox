---

- name: Copy configuration template
  notify: Restart postfix
  ansible.builtin.template:
    src: "{{ file }}"
    dest: "/etc/postfix/{{ file }}"
  loop:
    - master.cf
    - main.cf
    - ldap-aliases.cf
    - senders-bcc.cf
    - headers-check.cf
  loop_control:
    loop_var: file
