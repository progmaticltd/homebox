---

- name: Remove postfix packages
  apt:
    name: '{{ postfix_pkgs }}'
    state: absent
    purge: true
    autoremove: true

- name: Reinstall exim packages
  apt:
    name: '{{ exim_packages }}'
    state: present
