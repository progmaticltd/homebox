# Do not log local connections (e.g. from prometheus blackbox exporter)
:rawmsg,contains,"connect from main.{{ network.domain }}" stop
:rawmsg,contains,"Anonymous TLS connection established from main.{{ network.domain }}" stop
