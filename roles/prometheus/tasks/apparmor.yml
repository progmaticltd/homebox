---

- name: Install nginx AppArmor profile
  notify:
    - Activate AppArmor profile
    - Restart AppArmor service
    - Restart nginx
  template:
    src: apparmor.conf
    dest: /etc/apparmor.d/local/nginx-prometheus
  tags: apparmor

- name: Check if AppArmor nginx configuration already contains the line
  register: line_found
  shell: >-
    grep -c 'include <local/nginx-prometheus>'
    /etc/apparmor.d/usr.sbin.nginx
  changed_when: false
  failed_when: false
  tags: apparmor

- name: Add sogo AppArmor specific configuration
  when: line_found.stdout == '0'
  notify:
    - Activate AppArmor profile
    - Restart AppArmor service
    - Restart nginx
  lineinfile:
    path: /etc/apparmor.d/usr.sbin.nginx
    line: '  #include <local/nginx-prometheus>'
    insertbefore: '# End of local includes for homebox'
  tags: apparmor
