---

- name: Disable fail2ban for dovecot
  notify: Restart fail2ban
  community.general.ini_file:
    path: /etc/fail2ban/jail.conf
    option: enabled
    section: '{{ section }}'
    value: false
    create: no
  loop:
    - dovecot
    - sieve
  loop_control:
    loop_var: section
