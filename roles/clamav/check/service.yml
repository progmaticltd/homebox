---

- name: Check if clamav has been installed
  ansible.builtin.shell: dpkg -s clamav

- name: Check if the clamav services are up and running
  ansible.builtin.shell: 'systemctl status {{ service }}'
  loop:
    - clamav-daemon
    - clamav-freshclam
  loop_control:
    loop_var: service
