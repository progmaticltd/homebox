---

- name: Remove logs folder
  file:
    path: '{{ path }}'
    state: absent
  loop:
    - /var/log/clamsmtp
    - /var/log/clamav
  loop_control:
    loop_var: path

- name: Remove lib store folder
  file:
    path: /var/lib/clamav
    state: absent
