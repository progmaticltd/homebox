---

- name: Disable AppArmor profiles
  notify: Restart AppArmor service
  command: 'aa-disable {{ aa_config }}'
  loop:
    - usr.bin.freshclam
    - usr.sbin.clamd
    - usr.sbin.clamsmtpd
  ignore_errors: true
  loop_control:
    loop_var: aa_config

- name: Remove clamav AppArmor profiles
  file:
    path: '/etc/apparmor.d/{{ aa_config }}'
    state: absent
  loop:
    - usr.sbin.clamsmtpd
    - usr.bin.freshclam
    - usr.sbin.clamd
  loop_control:
    loop_var: aa_config
