---

- name: Install xkcdpass to generate the user passwords
  apt:
    name: xkcdpass
    state: present

- name: Create the ldap config changes folder
  file:
    path: /etc/ldap/changes
    state: directory
