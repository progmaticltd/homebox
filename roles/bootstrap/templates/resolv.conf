# Managed by homebox (role {{ role_name }})
{% for ns in network.dns.servers %}
nameserver {{ ns }}
{% endfor %}

# Search and domain options
domain {{ network.domain }}
search {{ network.domain }}

# Tuning options
options {{ network.dns.options | join(" ") }}
