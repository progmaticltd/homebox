---

- name: Create the certificate
  vars:
    certificates:
      - name: mta-sts
  include_role:
    name: certificates

- name: Copy the certificate update hook
  copy:
    src: renew-cert.sh
    dest: /etc/lego/hooks/mta-sts.{{ network.domain }}
    mode: 0700
