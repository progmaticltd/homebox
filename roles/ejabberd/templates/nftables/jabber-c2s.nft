#!/usr/sbin/nft -f
# Accept imap (143) and imaps(993)

table inet filter {

    chain input {

        tcp dport xmpp-client ct state new counter accept \
        comment "Accept XMPP connections from clients (TLS only)"

    }

}
