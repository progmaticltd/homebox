---

# Default grafana settings
grafana_default:
  dashboards:
    overwrite: false
    list:
      - name: system-load
      - name: nginx
      - name: postfix
      - name: pdns
      - name: certificates
      - name: sites
  # List of IPs that don't require authentication
  whitelist: [ ]
